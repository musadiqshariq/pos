import * as actionTypes from "./authTypes";

const INITIAL_STATE = {
    user: {}
}

export const Login_Reducer = (state = INITIAL_STATE, action) => {
    switch(action.type){
        default: 
        return state;
    }
}